  <%- include("header") -%>
  <!-- <%  if(template === 'weekend'){  %>
      <! above tags is scriplet tags which are used for conditioning a template check documentation for more details -->
      <!-- <h1 style = "color:yellow">The day is <%= template %> </h1>
  <%  } else{ %>
      <h1 style = "color:blue">The day is <%= template %> </h1>
  <%  } %> -->
  <div class="box">

    <h1 id = "heading"> <%= template %> </h1>

  </div>

  <div class="box">
    <% for(var i=0;i<newitem.length;i++){ %>
      <div class="item">
        <input type="checkbox">
        <p> <%= newitem[i] %> </p>
      </div>
    <% } %>
      <div class="item">
        <form action="/" method="post">
        <input type="text" name="firstinput" placeholder="New item">
        <button type="submit" name="button" value =<%= template %> > + </button>
        </form>
      </div>
  </div>

<%- include("footer") -%>
